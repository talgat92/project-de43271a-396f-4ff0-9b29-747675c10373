// Базовая конфигурация API мойки JAHAN (cars-wash.kz)
// Измените API_BASE_URL, если бэкенд расположен на другом домене.
export const API_BASE_URL = "https://cars-wash.kz/api";

// Номера боксов мойки
export const BAYS = [1, 2, 3, 4, 5, 6];

// Если true — при недоступности API интерфейс покажет демо-данные.
export const USE_DEMO_FALLBACK = true;
